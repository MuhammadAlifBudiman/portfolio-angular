# Task Breakdown: F007 — Professional Positioning & Copy Refresh

Feature ID: F007
GitHub Issue: TBD

## Tasks

- [ ] Confirm spec has no `[NEEDS CLARIFICATION]` markers.
- [ ] Owner reviews and approves new copy before any file is edited.
- [ ] Create implementation branch from `main`.
- [ ] Update `intro.role` in `en.json` and `id.json`.
- [ ] Update `intro.body` in `en.json` and `id.json` (remove "studying Informatics").
- [ ] Rewrite `about.p1`, `about.p2`, `about.p3` in `en.json` and `id.json`.
- [ ] Update `seo.*` keys in `en.json` and `id.json`.
- [ ] Update `src/index.html`: `<title>`, meta description, OG tags, Twitter tags, JSON-LD `jobTitle`.
- [ ] `npm run build` passes.
- [ ] `npm test -- --watch=false --browsers=ChromeHeadless` passes (no key-binding regressions).
- [ ] Manual UAT: hero and about copy in EN and ID; inspect HTML metadata in browser.
- [ ] Update `docs/TRACEABILITY_MATRIX.md` F007 row.
- [ ] Open PR linked to the issue.
